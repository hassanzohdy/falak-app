_Component({
                selector: 'flk-datepicker',
                c: 'Datepicker', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['name','id','placeholder','inputValue','isRequired'],
                children: {},
                render: function (component) {
                    let elQBweM = eo('div',null,null,`class`,`datepicker-wrapper`);
let elgkxIV = eo('div',null,null,`class`,`form-group`);
elgkxIV.cls = {'group-error': !!cfrmdlr.getError(component.name) };

            for (let className in elgkxIV.cls) {
                elgkxIV.classList.toggle(className, elgkxIV.cls[className]);
            }  
            let elXHgS0 = eo('label',null,null,`for`,`${component.id}`);
text(trans(component.inputs.getOption('label')));
let cndqEb3 = component.isRequired;
this.setState('stnzT_5', cndqEb3);
if (cndqEb3) { 
let elTw9E0 = eo('span','ETUf',null,`title`,`${trans(`required`)}`,`class`,`required`);
text(`*`);
ec('span');
}ec('label');
let elHRlJP = ev('input',null,null, eventListeners, {oninput:[function(e) {component.inputValue = this.value;},function(e) {let value = this.value.trim();if (component.isRequired && Is.empty(value)) {return cfrmdlr.addError(component.name, 'required', trans('validation.required'));}return cfrmdlr.removeError(component.name);}]},`value`,`${fval(component.inputValue)}`,`id`,`${component.id}`,`name`,`${(component.name).toInputName()}`,`placeholder`,`${component.placeholder}`,`type`,`text`,`class`,`${ component.inputs.getOption('class', '') }`);
component.input = elHRlJP;
elHRlJP.value = fval(component.inputValue);
let cndQKtB = cfrmdlr.getError(component.name);
this.setState('stw647P', cndQKtB);
if (cndQKtB) { 
let elGAqWh = eo('div','WWvf',null,`class`,`error-msg`);
text(cfrmdlr.getError(component.name));
ec('div');
}ec('div');
ec('div');

                    this.isReadyToGo();
                }
        });