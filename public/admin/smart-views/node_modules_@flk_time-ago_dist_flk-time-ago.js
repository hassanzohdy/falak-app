_Component({
                selector: 'flk-time-ago',
                c: 'TimeAgo', // c for component name
                unique: false, // flag to determine if the component object will be unique or not
                observable: true,
                observe: ['timestamp','displayedTime'],
                children: {},
                render: function (component) {
                    let elZ7_Ob = eo('span',null,null, 'style', this.__info.style || {}, eventListeners, mergeEvents({}, this.__info.events), boolAttrs, Object.merge({}, this.__info.boolAttrs || {}),...normalAttrs(Object.merge({id:`${component.timestamp}`}, this.__info.attrs || {})));
text(component.displayedTime);
ec('span');

                    this.isReadyToGo();
                }
        });