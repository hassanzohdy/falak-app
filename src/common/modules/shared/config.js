Config.extend({
    http: {
        endpoint: {
            baseUrl: 'https://site-nam.com/eapi',
            apiKey: '', // api key if required i.e Authorization: key {some-key}, add only the key without the `key` word
        }
    },
});